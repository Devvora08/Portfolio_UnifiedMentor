// the logic based here is implemented mostly for smooth functioning of the web page. no logic is applied here as such

document.addEventListener('DOMContentLoaded', function () {
    console.log('DOM fully loaded and parsed');
    //keeping check for load-page
});